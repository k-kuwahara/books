{
  "name": "books",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init": "gitbook init",
    "start": "gitbook serve",
    "build-js": "gitbook build js && rm -rf pages/js/ && cp -R js/_book/ pages/js/",
    "build-riot": "gitbook build riot && rm -rf pages/riot/ && cp -R riot/_book/ pages/riot/",
    "publish": "gh-pages -d pages -m 'auto publish'"
  },
  "author": "k-kuwahara",
  "license": "MIT",
  "devDependencies": {
    "gitbook-cli": "^2.3.2"
  },
  "dependencies": {
    "gh-pages": "^2.0.1"
  }
}
