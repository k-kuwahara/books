{
  "name": "books",
  "version": "0.2.0",
  "scripts": {
    "init": "gitbook init",
    "start:js": "gitbook serve js",
    "start:grid": "gitbook serve css-grid",
    "start:riot": "gitbook serve riot",
    "build:js": "gitbook build js && rm -rf pages/js/ && cp -R js/_book/ pages/js/",
    "build:grid": "gitbook build css-grid && rm -rf pages/css-grid/ && cp -R css-grid/_book/ pages/css-grid/",
    "build:riot": "gitbook build riot && rm -rf pages/riot/ && cp -R riot/_book/ pages/riot/",
    "publish": "gh-pages -d pages -m 'auto publish'"
  },
  "author": "kkeeth",
  "license": "MIT",
  "devDependencies": {
    "gitbook-cli": "^2.3.2"
  },
  "dependencies": {
    "gh-pages": "^3.1.0"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/kkeeth/books.git"
  },
  "bugs": {
    "url": "https://github.com/kkeeth/books/issues"
  },
  "homepage": "https://github.com/kkeeth/books#readme",
  "description": ""
}
